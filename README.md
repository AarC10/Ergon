# Ergon

Local-first multi-agent software engineering helper. CLI orchestrator that
runs Claude Code, OpenAI Codex, and Gemini around structured tasks, git
worktrees, and human-reviewed artifacts.

See `examples/` and `pyproject.toml`. Full docs land at the end of the MVP.
